namespace Qt.QSharp {
    /** This enum is passed to Widget's ctor to signal it is a base class. */
    public enum Derived {derived};
    public enum NativeArg1 {};
    public enum NativeArg2 {};
    public enum NativeArg3 {};
    public enum NativeArg4 {};
    public enum NativeArg5 {};
}
