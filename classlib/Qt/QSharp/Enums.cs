namespace Qt.QSharp {
    /** This enum is passed to a Class's ctor to signal it is a base class. */
    public enum QSharpDerived {derived};
    /** These enums are place holders for native data types. They should be used with CPPReplaceArgs. */
    public enum NativeArg1 {};
    public enum NativeArg2 {};
    public enum NativeArg3 {};
    public enum NativeArg4 {};
    public enum NativeArg5 {};
}
